# Local ingestion pipeline dependencies (NOT needed by the deployed
# Streamlit Cloud app — the app talks to Qdrant Cloud over HTTP and
# never partitions documents). Install with:
#
#     pip install -r requirements.txt -r requirements-ingest.txt
#
# The heavy document-parsing stack lives here so the deployed image
# stays lean (cold start on Streamlit Cloud is 5-10 minutes faster
# without unstructured/pdf2image/pdfminer/etc.).

# Core RAG ingestion pipeline
unstructured[md,pptx,docx,html]==0.18.11
python-magic-bin==0.4.14 ; sys_platform == "win32"
# pi-heif is imported unconditionally by unstructured.partition.pdf but isn't
# part of unstructured[pdf]'s required deps; ship it here so PDFs partition
# without dragging in the ~1 GB hi-res model stack (torch, transformers,
# unstructured-inference). We use strategy="fast" — no models needed.
pi-heif==0.22.0
pdf2image==1.17.0
pdfminer.six==20250506
pypdf==5.9.0
python-docx==1.2.0
python-pptx==1.0.2
lxml==5.3.0
markdown==3.7
beautifulsoup4==4.12.3
tqdm==4.66.5
